// usign an interface to store and return parsed data; so the code assumes the design / object properties.
interface IParsedData {
    firstName: string;
    lastName: string;
    clientId: string;
}

export {IParsedData};
